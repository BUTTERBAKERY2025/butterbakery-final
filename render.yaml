services:
  - type: web
    name: butterbakery
    env: node
    region: frankfurt
    plan: free
    buildCommand: npm install
    startCommand: node start.js
    envVars:
      - key: NODE_VERSION
        value: 18.12.1
      - key: NODE_ENV
        value: production
    autoDeploy: true